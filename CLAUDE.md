# Stepworks Main Site — Claude Rules

## Critical

- **NEVER read, write, or modify any files under `/Users/ryanbibb/dev/emr`** — that is a completely separate project and must never be touched.
- do see the sprint plan look in README.md file

## Commit Style

Use the format `type(scope): message`, e.g.:

- `feat(theming): add brand colors`
- `fix(calendar): rename table classname`
- `fix(tsconfig): add ES2017 target`

Common types: `feat`, `fix`, `refactor`, `style`, `chore`

## Components

- When possible use shadcn componenets
- use next Link for links instead of other tags

